# 지오코딩 DB 설정
PGHOST=
PGPORT=5432
PGDB=
PGUSER=
PGPASSWORD=
PGSCHEMA=geocoding

# 주소 테이블 (SELECT 권한만 필요)
BUILD_TABLE=match_build
JIBUN_TABLE=match_jibun

# 벌크 처리용 테이블 (INSERT, UPDATE, DELETE, SELECT 권한 필요)
BATCH_TABLE=addr_batch

# 이 건수 이상이면 자동으로 bulk 모드 사용 (기본값: 500)
BULK_THRESHOLD=500
